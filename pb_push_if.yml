---
- hosts: all
  connection: local
  gather_facts: no

  tasks:
  - name: push interface configure
    ios_config:
      src: "./configs/{{ inventory_hostname }}_if.cfg"
      provider: "{{ provider }}"
      authorize: yes